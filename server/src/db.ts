/**
 * Single import point for the generated Prisma client.
 *
 * Prisma 7 emits the client into `src/generated/prisma` rather than
 * `node_modules/@prisma/client`. Re-exporting here means application code never
 * has to know that path, and moving it later is a one-line change.
 */
export * from './generated/prisma/client';
