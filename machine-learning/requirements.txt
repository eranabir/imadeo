fastapi==0.121.2
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# The local recognition stack uses OpenCV Zoo's MIT/Apache-2.0 YuNet, SFace and
# NanoDet ONNX files. Their exact bytes are verified by app/model_store.py.
numpy==2.2.6
opencv-python-headless==4.11.0.86
pillow==11.3.0

# CLIP supplies visual search plus a second-stage embedding for individual pet
# grouping. The CPU wheel avoids pulling CUDA into a self-hosted image.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0
open_clip_torch==2.30.0
